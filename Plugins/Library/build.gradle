repositories {
    maven {
        name = 'Elytra'
        url = 'http://repo.elytradev.com'
    }
    maven {
        name = 'Modmuss'
        url = 'https://maven.modmuss50.me/'
    }
}

dependencies {
    // CurseForge
    provided "libvulpes:LibVulpes-${mc_version}-${version_libvulpes}:deobf"

    // Elytra
    deobfProvided "pl.asie.charset:charset:${version_charset}:full"

    // JitPack
    provided "com.github.SonarSonic:Sonar-Core:${commit_sonarcore}:deobf"

    // Modmuss Maven
    deobfProvided("RebornCore:RebornCore-${mc_version}:${version_reborncore}:universal") {
        exclude group: 'mezz.jei'
    }
}
